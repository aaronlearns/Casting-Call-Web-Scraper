from reader import testDescs

testingGroundDescs = list(testDescs.values())
# print(testingGroundDescs)

def findAverageWords(stringSet):

    stringWordAmounts = []
    # for string in stringSet:
    string_to_iterate = "Data Science"
    for char_index in range(len(string_to_iterate)):
        print(string_to_iterate[char_index+1])
    return
# findAverageWords(testDescs)

